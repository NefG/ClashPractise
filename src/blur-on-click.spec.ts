import { BlurOnClick } from './blur-on-click';

describe('BlurOnClick', () => {
  it('should create an instance', () => {
    const directive = new BlurOnClick();
    expect(directive).toBeTruthy();
  });
});
